/**
 * This function can be used program where you need to greet someone by their name :).
 * @param name - string - name you want to say hello
 */
export function sayHello(name: string): string {
  return `Hello ${name}!`
}